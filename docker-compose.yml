services:
  backend:
    build: ./backend
    container_name: voxely_backend
    expose:
      - "25401"
    restart: unless-stopped
  frontend:
    build: ./frontend
    container_name: voxely_frontend
    ports:
      - "${HOST_PORT:-3000}:3000"
    depends_on:
      - backend
    restart: unless-stopped